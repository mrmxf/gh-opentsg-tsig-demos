package main

import (
	"testing"
	//	. "github.com/smartystreets/goconvey/convey"
)

// Test the /reg path
func TestGetReg(t *testing.T) {
	/*

		test designs here, how do I set up a test for an obj.

		Send a writer that'll break it

	*/

	/*	maxAngle := 30.0
		maxTheta := (math.Pi / 180) * maxAngle

		out, _ := os.Create("./testdata/test1.obj")
		Sphere{0.1, 0.1, 10, maxTheta, maxTheta, 100, 100}.generate(out, io.Discard)

		out, _ = os.Create("./testdata/test2.obj")
		Sphere{0.5, 0.5, 5, maxTheta, maxTheta, 500, 500}.generate(out, io.Discard)*/

	/*
		Convey("Checking the "+path+" call returns the list of values", t, func() {
			Convey("Extracting the response from /reg with the parameters of "+param, func() {
				Convey("A register body matching ./testdata/reg/out.json is extracted with a status code of 200", func() {
					So(err, ShouldBeNil)
					So(expecErr, ShouldBeNil)
					So(res.StatusCode, ShouldResemble, http.StatusOK)
					So(string(regResponse), ShouldResemble, string(expec))
				})
			})
		})*/

}
